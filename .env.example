# ── Database (local Docker Postgres for now, Neon URL at deploy) ──
DATABASE_URL="postgresql://ecommerce:ecommerce_dev_password@localhost:5432/ecommerce_db"

# ── Redis (local Docker for now, Upstash URL at deploy) ──
REDIS_URL="redis://localhost:6379"

# ── Auth ──
# Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-random-32-plus-character-secret"
APP_URL="http://localhost:3000"

# ── Email (Resend) — needed for verification & password reset emails ──
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxx"
EMAIL_FROM="onboarding@resend.dev"

# ── Cloudinary (product image storage) ──
CLOUDINARY_CLOUD_NAME="d"
CLOUDINARY_API_KEY="5771462"
CLOUDINARY_API_SECRET="XJLmtlwlCnzaXTdhcuH1uQ"

# ── Admin seed account (used by prisma/seed.ts) ──
ADMIN_EMAIL="admin@nexora.com"
ADMIN_PASSWORD="Admin@12345"


GEMINI_API_KEY="your-gemini-api-key-here"

D7_API_TOKEN=""
D7_SENDER_ID=""